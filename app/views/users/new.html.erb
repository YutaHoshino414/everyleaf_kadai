<h1>ユーザー新規登録</h1><hr>

<% if @user.errors.any? %>
  <div id="error_explanation">
    <h2><%= @user.errors.count %>件のエラーがあります。</h2>
    <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="w-50">
<%= form_with(model: @user, local: true) do |f| %>  
  <%= f.label :name %>
  <%= f.text_field :name, placeholder: "名前を入力..", class:"form-control" %>
  <%= f.label :email %>
  <%= f.email_field :email, placeholder: "メールアドレスを入力..", class:"form-control" %>
  <%= f.label :password %>
  <%= f.password_field :password, placeholder: "パスワードを入力..", class:"form-control" %>
  <%= f.label :password_confirmation %>
  <%= f.password_field :password_confirmation, placeholder: "確認用パスワードを入力..", class:"form-control" %><br>
  <%= f.submit "アカウント作成",class: "btn btn-success" %>
<% end %>
</div>