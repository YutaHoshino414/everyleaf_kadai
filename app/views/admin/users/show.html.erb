<h2>ユーザー情報</h2>

<table class="table table-striped w-50">
  <tbody>
      <tr>
        <th>ユーザー名</th>
        <td><%= @user.name %></td>
      </tr>
      <tr>
        <th>メールアドレス</th>
        <td><%= @user.email %></td>
      </tr>
      <tr>
        <th>アカウント登録日</th>
        <td><%= @user.created_at.strftime('%Y-%m-%d %T') %></td>
      </tr>
      <tr>
        <th>管理者権限</th>
        <td><%= @user.admin %></td>
      </tr>
  </tbody>
</table><hr>
<h3>「<%= @user.name %>」のタスク一覧</h3>
  <table class="table">
    <tr>
      <th>タスク名</th>
      <th>タスク詳細</th>
      <th>終了期限</th>
      <th>登録日時</th>
      <th>ステータス</th>
      <th>優先度</th>
    </tr>
  <tbody>
    <% @tasks.each do |task| %>
    <tr>
      <td><%= task.name %></td>
      <td><%= task.content %></td>
      <td><%= task.deadline %>　　</td>
      <td><%= task.created_at.strftime('%Y-%m-%d %T') %></td>
      <td><%= task.status %>　　</td>
      <td><%= task.priority %>　　</td>
    </tr>
    <% end %>
  </tbody>
</table><hr>
<%= link_to "管理一覧画面へ", admin_users_path, class:'btn btn-info' %>